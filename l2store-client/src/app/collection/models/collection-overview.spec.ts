import { CollectionOverview } from './collection-overview';

describe('CollectionOverview', () => {
  it('should create an instance', () => {
    expect(new CollectionOverview()).toBeTruthy();
  });
});
